#pragma once
const float WINDOW_WIDTH = 500;
const float WINDOW_HEIGHT = 600;
const float OFFSET = 100;  // Отступ от начала экрана до игровой области

const float BLOCKS_PER_LINE = 10;  // Количество блоков в одной строке
const float BLOCKS_HEIGHT = 10;    // Высота блоков (ширина высчитывается)

const float PLAYER_WIDTH = 50;     // Ширина блока игрока
const float PLAYER_HEIGHT = 10;    // Высота блока игрока

const float BALL_RADIUS = 5;       // Радиус шарика